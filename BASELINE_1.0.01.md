# Tactic Echo 1.0.01 基线声明

当前 `1.0.01` 是唯一可继续开发的基线。其源码状态来源于已验证的 `0.9.51` 工作树；`0.9.0` 仅保留为历史维护线与版本演进依据。

本版本将受控自动爆发接管的安全边界写入 `AGENTS.md`：自动爆发只能在人工白名单、双开关、官方推荐与 TEAP 健康、默认动作条绑定确认和全部 TEK 门禁通过时临时接管主派发；任何异常或人工接管均立即清空队列并停止派发。
