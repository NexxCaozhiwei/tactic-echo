# Tactic Echo Addon Media

Place the TE addon icon here as:

```text
te-icon.png
```

If a WoW-native texture is needed later, export the same image as `te-icon.tga` or `te-icon.blp` and reference it from `!TacticEcho.toc`.

