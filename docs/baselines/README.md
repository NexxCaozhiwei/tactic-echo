# 基线文档

所有 `BASELINE_<版本>.md` 必须仅存放在本目录。

- 每次版本化源码改动都必须新增或更新当前版本的基线文档，并在根目录 `CHANGELOG.md` 写入同版本条目。
- 基线记录当前可执行边界、已确认的行为和不得回归的安全约束；历史版本仅作追溯，不得替代 live source。
- 根目录不得新增或保留 `BASELINE_*.md` 副本，以避免交付包出现多个“当前基线”。

当前版本：`BASELINE_1.1.2.md`（TEK 修饰键手动接管、自动启停待命状态、自动打断硬暂停与共享宏资格冻结）。
