# Patch Manifest Archive

所有 `PATCH_MANIFEST*` 交付清单只允许存放在本目录。

- 根目录不得保留 `PATCH_MANIFEST*` 副本。
- `docs/` 根层不得直接堆放 `PATCH_MANIFEST*` 文件；历史清单统一归档到本目录。
- 清单只记录对应版本覆盖内容、安装提示和不包含项；当前运行边界仍以 `AGENTS.md`、`docs/baselines/BASELINE_<VERSION>.md`、`CHANGELOG.md` 与 live source 为准。
